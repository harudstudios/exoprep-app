import 'package:flutter/material.dart';

class ExamDashboardView extends StatelessWidget {
  const ExamDashboardView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Exam Dashboard'),
      ),
    );
  }
}
